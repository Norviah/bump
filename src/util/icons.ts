import { Logger } from '@/structs/Logger';

/**
 * Icons used in the application.
 *
 * These are used to display a visual representation of the result to the user,
 * instead of just a simple string.
 */
export const icons = { CHECKMARK: Logger.Color('✓', 'green'), X: Logger.Color('✗', 'red') };
